services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=/data/sqlite.db
      - NODE_ENV=production
    volumes:
      - data:/data
    restart: unless-stopped

volumes:
  data:
