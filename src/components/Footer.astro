---
import { getCurrentLanguage, getLocalizedPath, useTranslations } from "../i18n";

const currentLang = getCurrentLanguage(Astro);
const t = useTranslations(currentLang);
const getPath = (path: string) => getLocalizedPath(path, currentLang);
---

<footer class="bg-[#16273b] text-white/80 py-12">
  <div class="max-w-7xl mx-auto px-6 grid md:grid-cols-3 gap-8">

    <!-- BRAND -->
    <div>
      <h3 class="font-semibold text-lg mb-3">ORO INCA Joyería</h3>
      <p class="text-sm">
        {t('footer.description')}
      </p>
    </div>

    <!-- LINKS -->
    <div>
      <h4 class="font-semibold mb-3">{t('footer.quickLinks')}</h4>
      <ul class="space-y-2 text-sm">
        <li><a href={getPath("/")} class="hover:text-[#c7a14a]">{t('nav.inicio')}</a></li>
        <li><a href={getPath("/products")} class="hover:text-[#c7a14a]">{t('nav.productos')}</a></li>
        <li><a href={getPath("/about")} class="hover:text-[#c7a14a]">{t('nav.nosotros')}</a></li>
      </ul>
    </div>

    <!-- CONTACT -->
    <div>
      <h4 class="font-semibold mb-3">{t('footer.contact')}</h4>
      <p class="text-sm">📍 Perú</p>
      <p class="text-sm">📞 +51 926 946 362</p>
    </div>

  </div>

  <div class="text-center text-xs text-white/50 mt-10">
    © {new Date().getFullYear()} ORO INCA Joyería. {t('footer.rights')}
  </div>
</footer>
